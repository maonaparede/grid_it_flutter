

import 'package:flutter/material.dart';

final Color colorAll = Colors.red;
final double sizeAll = 40.0;

Icon photo = Icon(
  Icons.add_photo_alternate_outlined,
  color: colorAll,
  size: sizeAll,
);

Icon crop = Icon(
  Icons.crop,
  color: colorAll,
  size: sizeAll,
);

Icon brush = Icon(
  Icons.brush,
  color: colorAll,
  size: sizeAll,
);

Icon save = Icon(
  Icons.save,
  color: colorAll,
  size: sizeAll,
);